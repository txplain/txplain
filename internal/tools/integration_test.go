package tools

// Integration tests have been moved to the root-level integration_test.go file
// to avoid import cycle issues with the agent package.
// Run integration tests with: go test -v ./... (from project root)